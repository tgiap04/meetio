{
  "name": "@meetio/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contract types for Meetio (types only — zero runtime dependencies).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests in packages/shared (types only)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
