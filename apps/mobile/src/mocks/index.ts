/**
 * Design-sourced placeholder content for screens 04–14. Every value here is
 * transcribed from `plans/260921-1012-mobile-ui-screens-from-design/design/`
 * — nothing is invented, and nothing here is real user data. Low-confidence
 * transcriptions and the one inferred row are flagged in-source with
 * `// LOW CONFIDENCE:` / `// INFERRED:` comments; see the phase 02 hand-back
 * for the collected list.
 *
 * No API calls, no async, no state — pure compile-time constants.
 */

export * from './types';

export { MEETINGS } from './meetings.mock';
export { TRANSCRIPT_LINES } from './transcript.mock';
export { MEETING_SUMMARY, ACTION_ITEMS } from './meeting-detail.mock';
export { AUDIO_SOURCE_OPTIONS, RECORDING_SETTINGS_DEFAULTS } from './recording-options.mock';
export { SETTINGS_ENTRIES, ABOUT_MEETIO_ENTRIES } from './settings-entries.mock';
