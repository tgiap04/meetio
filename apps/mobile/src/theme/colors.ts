/**
 * Minimal color palette for the Phase 06 screens. Expanded per DESIGN.md as
 * later phases add screens; kept intentionally small here (YAGNI) since this
 * phase's job is the app shell, not the full visual system.
 */
export const colors = {
  background: '#FFFFFF',
  surface: '#F5F7FA',
  border: '#D6DBE1',
  text: '#111318',
  textMuted: '#5B6472',
  primary: '#2F6FED',
  primaryText: '#FFFFFF',
  danger: '#D64545',
  success: '#1E8E5A',
} as const;
