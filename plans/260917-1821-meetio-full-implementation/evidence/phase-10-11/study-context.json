{
  "task": "Phase 10 + 11: transcript read/edit, scoped reindex, Markdown/HTML export, pipeline infrastructure (per-step BullMQ queues, orchestrator, retries, pause at unimplemented steps, status endpoint, realtime status, push notification, Gemini client with usage tracking) and wiring the existing mobile screens to the real API",
  "mode": "interactive",
  "acceptanceCriteria": [
    "transcript pages by seq and segments can be corrected only when the meeting is not live or processing",
    "editing never re-runs the pipeline by itself and the meeting reports unprocessed edits",
    "reindex changed after edits is scoped to the edit window and reindex after failure skips succeeded steps",
    "export produces Markdown and HTML with chosen sections and a note when the summary is not ready",
    "ending a meeting starts a pipeline run that executes steps in order and never reports ready without all steps succeeding",
    "a failing step is retried 3 times with growing waits and then fails the meeting naming the step",
    "the meeting ready push is sent exactly once per meeting with generic text and honours the user setting",
    "every Gemini call records a usage row with token counts and a spent budget blocks calls",
    "mobile library, detail and transcript screens use the real API including delete undo and export"
  ],
  "touchpoints": [
    "apps/api/src/pipeline/",
    "apps/api/src/transcript/",
    "apps/api/src/export/",
    "apps/api/src/notifications/",
    "apps/api/src/ai/",
    "apps/api/src/meetings/",
    "apps/api/src/jobs/",
    "apps/api/src/database/migrations/1758000000014-AddPipelineRunsEditsAndPushTokens.ts",
    "packages/shared/src/meetings/",
    "packages/shared/src/notifications/",
    "apps/mobile/"
  ],
  "blastRadius": [
    "meeting-processing job id format changed to meeting-run",
    "end and abandoned sweep now increment pipeline_run",
    "PATCH /meetings blank title now reverts to default instead of 400",
    "BullMQ root config reads BULLMQ_PREFIX",
    "e2e harness isolation from a running dev server"
  ],
  "contracts": [
    "api-spec section 3 status, reindex, export",
    "api-spec section 4 GET segments and PATCH segment",
    "api-spec section 2 push tokens",
    "WebSocket processing_status and meeting_ready payloads"
  ]
}
